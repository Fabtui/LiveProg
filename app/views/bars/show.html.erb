<div class="backlink">
  <%= link_to :back do %>
    <i class="fas fa-arrow-left"></i>
  <% end %>
</div>
<div class="bar-show-container">
  <div class="bar-show-banner" >
    <h1><%= @bar.name %></h1>
    <i class="far fa-star"></i>
  </div>
  <div class="bar-show-con">
    <div class="bar-show-photo">
      <% @bar.photo.each do |photo| %>
        <%= cl_image_tag photo.key, :width=>400, :height=>250, 'aria-expanded': false %>
      <% end %>
    </div>
    <div class="bar-show-tags">
      <a class="btn btn-ghost" href="#">Write a story</a>
      <a class="btn btn-ghost" href="#">Write a story</a>
      <a class="btn btn-ghost" href="#">Write a story</a>
    </div>
    <div class="bar-show-description"> <p><%= @bar.description %></p> </div>
    <div class="bar-show-programme">
      <div>date à venir</div>
      <%@events.each do |event|%>
        <%=link_to(event_path(event), class:"btn btn-gradient") do %>
          <%=event.name%>
          <%=event.start_date%>
        <%end%>
      <%end%>
    </div>
  </div>
</div>



<% if false %>
<p><%= @bar.address %></p>
<p><%= @bar.menu %></p>
<% end %>
