var popUp = document.getElementById('participation-popup')
var container = document.querySelector('#infos-container')
popUp.classList.remove('hidden')
container.style.filter = "blur(3px)";

var link = document.getElementById('add-participation')
console.log(link)

link.outerHTML = "<%= link_to "Ajouter!", event_participations_path(@event.id), method: :post, remote: true, id: "add-participation", class: "links-event" %>"
